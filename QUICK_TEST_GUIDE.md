# 快速测试指南

## 🚀 快速开始

### 1. 打开应用
在浏览器中打开 `index.html`

### 2. 打开浏览器控制台
- Windows/Linux: `F12` 或 `Ctrl+Shift+J`
- Mac: `Cmd+Option+J`

### 3. 运行自动化测试

```javascript
// 运行所有测试
await runAllTests();

// 或者单独运行每个测试
await testAblyMessaging();      // 测试 Ably 消息功能
await testPusherMessaging();    // 测试 Pusher 消息功能
await testReconnection();       // 测试重连机制
await testNotifications();      // 测试通知功能
```

## 📋 手动测试步骤

### 测试消息收发（最重要）

1. **准备两个窗口：**
   - 打开两个浏览器窗口/标签
   - 在第一个窗口配置设备名称："设备A"
   - 记录频道名称（或使用自定义名称）
   - 在第二个窗口使用相同频道名称，设备名称："设备B"

2. **发送消息：**
   - 在设备A发送："Hello from A"
   - 在设备B应该立即看到消息
   - 在设备B发送："Hello from B"
   - 在设备A应该立即看到消息

3. **验证：**
   - ✅ 消息在3秒内到达
   - ✅ 显示正确的设备名称
   - ✅ 显示时间戳
   - ✅ 发送的消息在右侧（蓝色）
   - ✅ 接收的消息在左侧（灰色）

### 测试服务切换

1. **切换到 Pusher：**
   - 点击设置按钮（⚙️）
   - 选择"Pusher"
   - 点击保存
   - 等待重新连接

2. **测试消息：**
   - 发送一条消息
   - 验证消息正常收发

3. **切换回 Ably：**
   - 重复上述步骤，选择"Ably"

### 测试重连

1. **断开网络：**
   - 打开 DevTools (F12)
   - Network 标签 > Throttling > Offline

2. **观察：**
   - 状态变为"未连接"

3. **恢复网络：**
   - Throttling > Online
   - 等待5秒
   - 状态应变为"已连接"

### 测试通知

1. **授予权限：**
   - 首次打开应用时允许通知

2. **测试：**
   - 最小化接收端窗口
   - 从发送端发送消息
   - 应该看到桌面通知

## 🔍 检查存储

```javascript
// 查看配置
const config = JSON.parse(localStorage.getItem('messaging_app_config'));
console.log('配置:', config);

// 查看消息
const messages = JSON.parse(localStorage.getItem('messaging_app_messages'));
console.log('消息数量:', messages.length);
console.log('最新消息:', messages[messages.length - 1]);

// 清除所有数据（重置应用）
localStorage.clear();
location.reload();
```

## ✅ 测试检查清单

- [ ] Ably 消息收发正常
- [ ] Pusher 消息收发正常
- [ ] 服务切换功能正常
- [ ] 自动重连功能正常
- [ ] 通知功能正常
- [ ] 消息存储正常
- [ ] 历史消息加载正常

## 📊 预期测试结果

运行 `await runAllTests()` 后，应该看到：

```
🚀 Running All Tests for Cross-Device Messaging
==================================================

[各项测试输出...]

==================================================
ALL TESTS COMPLETED
==================================================
Total Tests: 20+
Total Passed: 20+
Total Failed: 0
Overall Success Rate: 100.00%
==================================================
```

## 🐛 常见问题

### 问题：测试工具未加载
**解决：** 刷新页面或手动加载：
```javascript
const script = document.createElement('script');
script.src = 'js/test-utils.js';
document.body.appendChild(script);
```

### 问题：消息未收到
**检查：**
1. 两个窗口使用相同的频道名称
2. 连接状态为"已连接"
3. 浏览器控制台是否有错误

### 问题：通知不显示
**检查：**
1. 通知权限是否授予
2. 接收端窗口是否未聚焦
3. 浏览器是否支持通知

## 📖 详细文档

查看 `TESTING.md` 获取完整的测试指南和详细说明。
